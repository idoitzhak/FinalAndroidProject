<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.FinalAndroidProject"
        tools:targetApi="31">
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <meta-data
                android:name="android.app.lib_name"
                android:value="" />
        </activity>
        <activity android:name=".Countries"
            android:exported="false"
            android:label="Countries" />
        <activity android:name=".Country"
            android:exported="false"
            android:label="Country" />
        <activity android:name=".CountryAdapter"
            android:exported="false"
            android:label="Country Adapter" />
        <activity android:name=".SearchItem"
            android:exported="false"
            android:label="Search" />
        <activity android:name=".CreateSong"
            android:exported="false"
            android:label="Create" />
        <activity android:name=".DeleteItem"
            android:exported="false"
            android:label="Delete" />


    </application>

</manifest>